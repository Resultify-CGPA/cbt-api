<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      @page {
        size: auto;
        margin: 25mm 25mm 25mm 25mm;
      }
      body {
        margin: 0px;
      }

      div.table h2 {
        text-align: left;
      }

      th,
      td {
        font-size: 14pt;
        padding: 10px;
        text-transform: capitalize;
      }

      div.hrule {
        border: 0.5px solid #000;
        width: 170px;
      }
    </style>
  </head>
  <body style="font-family: Arial, Helvetica, sans-serif;">
    <% Object.values(datas).forEach(data => { %>

    <div style="display: flex; justify-content: center;">
      <img src="<%= logo %>" alt="Logo" />
    </div>
    <h1
      style="
        text-align: center;
        font-size: 1.5rem;
        line-height: 45px;
        margin-top: 0;
        margin-bottom: 50px;
      "
    >
      Prince Abubakar Audu University, Anyigba <br />
      e-Examination <br />
      <span style="text-transform: capitalize;"><%= metaData.examTitle %></span>
    </h1>

    <div>
      <h2 style="font-size: 18pt; text-transform: capitalize;">
        <%= data.faculty %>
      </h2>
      <table border="1" style="border-collapse: collapse;">
        <thead>
          <tr>
            <th>S/N</th>
            <th>Name</th>
            <th>Jamb Reg.No</th>
            <th>Faculty</th>
            <th>Department</th>
            <th>JAMB Ratio</th>
            <th>PUME Ratio</th>
            <th>Total Ratio</th>
          </tr>
        </thead>
        <% if (data.results.length > 0) { %> <% data.results.forEach(function
        iterator(student, i) { %>
        <tr>
          <td><%= i+1 %></td>
          <td><%= student.name %></td>
          <td style="text-transform: uppercase;"><%= student.matric %></td>
          <td><%= data.faculty %></td>
          <td><%= student.department %></td>
          <td><%= student.ca %></td>
          <td><%= student.exam %></td>
          <td><%= student.ca + student.exam %></td>
        </tr>
        <% }); %> <% } %>
      </table>
      <div
        style="display: flex; justify-content: space-between; margin-top: 80px;"
      >
        <div
          style="display: flex; flex-direction: column; align-items: center;"
        >
          <div class="hrule"></div>
          <h4>e-Examination Chairman</h4>
        </div>
      </div>
      <div style="break-after: page;"></div>
    </div>
    <% }); %>
  </body>
</html>
